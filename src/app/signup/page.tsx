// Phase 0 stub — this page is replaced in Phase 2 by /[locale]/getcred and /[locale]/become-evaluator
import { redirect } from "next/navigation";

export default function SignUpPage() {
  redirect("/en/login");
}
